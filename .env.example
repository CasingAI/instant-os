# 复制为 .env 后按需填写；.env 已被 git 忽略，不会提交。
# 本项目使用 pnpm；pnpm dev 启动本地开发（端口 6173）。

# ---- Debug 模式预置配置（仅当 dev 构建且以 ?debug=1 启动时启用）----
# 默认使用 OpenCode Go（https://opencode.ai/zen/go/v1，模型 deepseek-v4-flash）；只需填入 API Key。
# 如需换模型（如 grok-4.5），可用 VITE_DEBUG_OPENAI_MODEL 覆盖：# VITE_DEBUG_OPENAI_MODEL=grok-4.5
# 用途：以 ?debug=1 绕过初始化直接进桌面做功能测试时，无需手动走「钥匙串」配置 AI 账户。
# 钥匙串未配置时，这些值会作为默认值写入钥匙串；钥匙串已配置时以钥匙串为准，忽略此处。
# 说明：生产构建（pnpm build / pnpm preview）下这些变量不生效。
VITE_DEBUG_OPENAI_API_KEY=

# Debug 模式下代理服务器（仅 ?debug=1 且 dev 构建生效；不写系统设置）
# 取值：shared（Instant 共享，默认兜底）| off（关闭）| 自定义 Worker URL
# 未设置时：沿用系统代理设置；若系统也未开启则自动使用 Instant 共享。
# VITE_DEBUG_OPENAI_PROXY=shared
