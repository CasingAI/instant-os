# 复制为 .env 后按需填写；.env 已被 git 忽略，不会提交。
# 本项目使用 pnpm；pnpm dev 启动本地开发（端口 6173）。

# ---- OpenAI 兼容配置（env 兜底）----
# 免费额度网关默认启用（Instant 免费额度），无需配置 key 即可使用 AI。
# 如需使用自己的 key，可在「系统设置 → 账户」钥匙串中配置，或在此填写作为兜底：
# VITE_OPENAI_API_KEY=
# VITE_OPENAI_BASE_URL=
# VITE_OPENAI_MODEL=

# ---- Virtual Machine 运行时（独立源，生产默认 https://vm.casing-ai.com/）----
# 生产构建默认使用 https://vm.casing-ai.com/。
# 开发时默认用对侧 loopback（页面在 localhost 则 iframe 走 127.0.0.1，反之亦然），避免同源渲染进程被 V86 卡住。
# VITE_VM_RUNTIME_ORIGIN=http://127.0.0.1:6175
