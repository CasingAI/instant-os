# AI 用量统计

全局 AI 用量**不是**在 OpenAI SDK 层统一劫持的，而是各调用点显式接入。新增或修改 AI 请求时，先确认该路径是否已计入统计；漏接则不会出现在「AI 用量」里，且通常不会报错。

具体接入方式、调用点清单、存储位置等，动手前自行在代码库里调查。

# pnpm

本项目使用pnpm进行包管理


# 测试

## 浏览器测试

如果用户没有显示要求，则不使用浏览器或无头浏览器和其他类似的工具进行自动化测试

## DebugMode

当用户要求你在 DebugMode 中进行自动化测试时，由于本项目禁用了热更新，所以对于 AI 来说，最佳的让浏览器的网页强制重载到最新代码的建议方案是直接 kill 当前的 dev Server 并立刻重启一个新的，此时浏览器会自动强制重载所有的本项目的页面。

## 虚拟机实机测试

做 Instant OS / 虚拟机相关的实机调试前，先读 `~/.zcode/notes/vm-zdebug-testing-notes.md`（VM / zdebug 测试要点备忘：截图通道、UI 自动化配方、刷盘/档位坑、账本漂移事故等），按需取用，不需要全文背读。要点速记：

- **每操作一次，必须看一次截图**（canvas 截屏核对实际画面）；点「开机/关机」前先确认选中的是哪台机器。
- **单实例**：任何时刻最多一台虚拟机在运行；「关机后写入」档必须等状态回「已停止」才能开下一台。
- 调试桥 `/eval` 按页签 ID 定向、不是广播；页签归属靠截图判断。
- 残留缓存的界面文案是「上次会话的改动尚未写入硬盘镜像」+「去急救」；差量缓存走合并，不要删。

### 数据空间账本

- 容量断言（`FilesStorageFullError`「16.0 GB 上限」）读的是 `instant-os-files` IDB `meta.byte-total` 累计计数器，不是真实实占；计数器会漂移（2026-09-14 事故：账面 15.9 GB、真实实占 6.56 GB），根修未做。
- 诊断：扫 blobs store 按 `storedByteSize` 求和 = 真实实占，与 `meta.byte-total` 对比；临时修法是把 `byte-total` 重写为实测值。
- 关机刷盘失败留下「虚拟机硬盘缓存」差量附加；空间恢复后开机「去急救 → 合并」写回镜像，不要删缓存（删了丢改动）。