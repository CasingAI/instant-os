# Instant OS 2.0.0 更新日志（草稿）

相对 `main`（**1.2.0**）→ 当前 `experimental`（**2.0.0**）。

统计大约是：630+ 次提交，源码约 10 万行量级的净增，另有大量提示音、本地模型与许可证资源。下面按产品能力整理，不是逐条 commit 翻译。请先核对口径，再决定要不要进仓库。

---

## 一句话

1.2 更接近「生成式桌面」：应用集市、AI 网页、邮件、新闻由模型现场写出来。

2.0 把 Instant OS 做成更完整的 **AI Native 系统**：AI 是一等系统资源（免费额度默认开、钥匙串、用量、事件、Agent），人和模型共用同一套窗口、文件、终端和网络。因此会同时出现「内容可以生成」和「可以打开真网页、改真实文件、跑 VS Code Agent」——不是两套产品叠在一起，而是同一套操作系统补全了环境和默认模型。打开即桌面，不再卡在设置向导。

---

## 开箱方式

- **免费额度默认开启**：内置 Instant 共享 AI（PoW challenge），不必先填自己的 API Key。
- **打开即桌面**：去掉初始化向导和 Debug 模式；首次启动改出「欢迎中心」（另有并列的「欢迎」「你好」入口，不进桌面图标）。
- 仍可在钥匙串里加自己的供应商；环境变量设置页已拿掉，默认 env 改为代码常量。
- 大模型权重默认从 **R2 网关**下载，也可按哈希导入缓存；可在开发者选项里改成同源 `/assets`。

---

## 新应用

| 应用 | 做什么 |
|------|--------|
| **Chromo** | 真网页浏览器（virtual-chromo + 云服务代理）。书签、历史、会话；拟物 Chrome 外壳；DevTools（控制台 / 网络 / 应用程序，可独立窗口）。图标带「开发中」套子。 |
| **WebView / 开发者工具** | 终端可驱动的离屏网页单元；Page Host 的独立调试窗。 |
| **文件信息** | 从「文件」右键「显示信息」：属性分节、批量摘要、多标签。 |
| **文稿（Pages）** | 飞书式 Markdown 块编辑；原生 `.pages` 包；表格 / 公式 / 网格视图；斜杠插入。 |
| **音乐** | 曲库绑定用户目录「音乐」；播放器、逐字卡拉 OK、分轨可视化。 |
| **音乐实验室** | 本地 HTDemucs 分轨、MDX 人声增强、SenseVoice / Zipformer 歌词对齐、节拍、`.stems.zip` 工程包。 |
| **ProDude** | 对话式编程助手，默认在 `/user` 改文件。 |
| **GitHub Desktop** | 用 GitHub API 在 `/dev/github/…` 维护工作树：克隆、分支、commit / 推送 / 拉取、stash、AI 提交说明。不是真实 `.git`。 |
| **终端** | QuickJS + 薄 Node 兼容层的真 JS REPL；可操作 VFS、开应用、驱动 WebView。原类 Unix 壳改名为「模拟终端」并标弃用。 |
| **Virtual JS** | 按窗口隔离的 QuickJS 实例演示（不接 Node / VFS / npm）。 |
| **包管理** | Instant npm 的 GUI：项目依赖 + 全局 CAS 缓存（`/dev/npm`）。 |
| **压缩包实用工具** | 浏览 / 解压 / 打包 zip、tar、gz。 |
| **空间嗅探** | Treemap 看占用；可挂载本机目录再扫。 |
| **注册表** | 从设置里拆成独立应用：按应用命名空间看、改、下钻 JSON。 |
| **服务** | 后台 Worker（Tokenizer、搜索、TS 解析等）的启动类型与启停。 |
| **欢迎中心 / 欢迎 / 你好** | 可再打开的导览，引导先配钥匙串再试用应用。 |
| **评测（AttuneBench）** | 用公开情商基准批量打你配置过的文本模型。 |
| **MIDI 演示** | 自然语言 → ABC 乐谱 → 钢琴 MIDI。 |
| **LLM Playground** | 自由拼消息、调采样参数的请求调试台。 |
| **SRML 演示** | 标签 DSL / Fork 的格式遵循实验。 |
| **UI 组件库** | 系统控件的可交互 Demo。 |

---

## 既有应用：明显变大的几块

### Virtual Studio Code Desktop

从「Monaco + 侧栏」扩成带 Agent 的工作台：

- Ask / Plan / Agent（旧 Edit 已去掉）；模型可 `switch_mode`。
- Sub Agent：独立终端、会话持久化、主 Agent 可追问。
- 工作区搜索 / 替换、Search Editor、内联 Tab 补全。
- 图片附件 + vision 子 Agent；长按空格听写。
- 多层上下文压缩、空闲超时与重试、工具输出溢出到 session tmp。
- 经 `instant.git` 操作 GitHub 工作树；遵守终端只读 / 受控 FS。
- JSONL 预览、设置改成分层导航（外观 / AI / 编辑）。

### 文件

- 每个应用独立数据目录；生成应用本体落到 `/Applications/{id}.app/Contents`，数据在 `…/Data`。
- `/user` 下固定 Downloads、Musics、Pictures；`/tmp` 临时卷；工作区级 tmp。
- 可挂载本机文件夹；列表视图、排序、键盘导航、压缩 / 解压、大文件分块。
- 默认程序坞最左是「文件」，且不可移除。

### 网页浏览器（AI）

还是「不请求真实站点、由 AI 画页面」。和 Chromo 并存：一个想象互联网，一个经代理看真网页。

### 钥匙串 / 账户

供应商面变宽：OpenRouter、火山方舟 Coding / Agent Plan、OpenCode Go / Zen、GitHub Token、基座 / 副基座分类、定价与词表匹配。免费额度与自备 Provider 走同一套配置模型。

### 系统设置

一级菜单分组（通用 / 存储 / 外观 / 系统 / 账户 / 网络与内容 / 云服务 / 开发者）。新增：云服务（原代理服务器）、启动项、背景刷新、NPM、模型缓存、还原桌面布局、文件存储占用分析等。环境变量页删除。

### 性能监视器（原任务管理器）

「查看信息」会跳到对应页；加上系统服务 Worker 堆、云服务 / 文件吞吐指标。

---

## 桌面、窗口与外观

- **Flip 3D**：点桌面空白或按住（默认进叠层）；触控板切窗；连按跟手；退出只聚焦最前那张。
- 新建窗口先盖一层图标启动层，满约 400ms 再揭开。
- 内置应用**按窗口懒加载**，进桌面不再把 Monaco 一起带上；启动期把大量小 JS 合成几个 boot 包。
- 图标装饰：右上角「AI」丝带、底部「开发中」套子；一批图标改成拟物 SVG。
- 开发工具默认收进文件夹；新内置 App 会自动补到桌面末尾。
- 系统通知改成统一模板；划掉横幅后其余通知立刻贴上去。
- 提示音资源入库（多套风格），设置页可试听；主音量仍是全局乘数。
- 菜单栏云服务、电量、音量弹层收成 Aqua 风格。

---

## 文件系统、存储与运行时

- 内置应用存储迁入 **App Registry**，按字段拆 key。
- 数据空间硬上限提到 **8 GB**。
- VFS：写时复制、路径级 watch、范围读 / 分块、`instant.grep`。
- **QuickJS**：会话级实例、CJS / ESM、fs 桥、薄 Node 内建（path / buffer / stream / crypto / zlib / fetch 经代理 等）。
- **Instant npm**：CAS store + 符号链接 `node_modules`、锁优先、可选 lifecycle 脚本；不是官方 npm，不跑原生 addon。
- **系统服务监督器**：Worker 按自动 / 延迟 / 手动 / 禁用拉起。
- 统一 AI 推理调度：同时只驻留一个本地模型，空闲卸载（分轨 / 对齐 / 语音等）。

---

## AI 与云服务

- 代理服务器升级为 **云服务**：菜单栏状态、Challenge 进度；GitHub 克隆、Chromo、部分供应商要先连上。
- 免费额度：PBKDF2 challenge，Web Worker 可并行求解；不再强制「必须走代理才能用免费模型」那套旧开关。
- AI 用量增加 **按模型分类** 和 **prompt cache 命中率**（只统计真正读到过缓存的请求）。
- Agent：上下文压缩、Sub Agent、system-reminder、流式空闲超时。

---

## 构建与其它

- Vite HMR 从源头关掉（`hmr: false`）。
- onnxruntime wasm 构建时 gzip，运行时再解压，躲开 Cloudflare Pages 单文件 25 MiB 限制。
- 版本号 `1.2.0` → `2.0.0`。

---

## 移除 / 弃用

- **照片**应用
- **视角实验室**
- 初始化设置向导、Debug 模式
- 设置里的环境变量页
- 独立「歌词对齐」App（能力收进音乐实验室；`src/apps/align` 仍是引擎）
- **模拟终端**：保留但弃用，请用「终端」
- 验证用测试图标应用（`test-ribbon` / `test-sleeve`），装饰机制留下

---

## 和 README 的关系

根目录 `README.md` 已按 **AI Native 操作系统** 的口径改过，和这份日志对过。若你改产品表述，两边最好一起改。
