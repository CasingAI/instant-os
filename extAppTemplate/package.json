{
  "name": "instant-os-ext-app-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-dist-artifacts.mjs --public && vite",
    "build": "tsc -b && vite build && node scripts/generate-dist-artifacts.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^24.12.3",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
