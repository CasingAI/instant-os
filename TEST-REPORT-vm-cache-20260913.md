# 虚拟机磁盘缓存 手工验收 夜间执行报告

- 日期：2026-09-12 晚 ~ 2026-09-13 凌晨（自动执行）
- 对象：仅「虚拟机XP 新」（其余 VM 未做任何测试性操作；TinyCore 档位已按清理清单恢复）
- 验收计划：`/Users/john/.cursor/plans/vm_cache_手工验收_3304cf68.plan.md`
- 结论速览：~~整体未通过~~ → **P0 已修复并复测通过（2026-09-13 下午复测，见文末「七、P0 修复后复测报告」）**。原始问题：XP 安装完成并进入桌面后，经一次正常断电，虚拟硬盘的引导文件（NTLDR）数据被无关内容覆盖，系统自此无法引导。修复后的落盘路径经「重装 → 干净关机 → 合并 → 重启 → 再次关机 → 再重启」多轮验证，镜像完好。

---

## 一、总体判定

| 验收项 | 判定 | 说明 |
|---|---|---|
| ① 三档互切与断电确认文案 | **部分通过** | 「尽快写入」断电文案实机验证 ✓；三档切换/持久化/列表徽标实机验证 ✓；「关机后写入」「不保存」断电文案仅源码核验（虚机无法引导，实机弹窗未能再次触发） |
| ② 尽快写入硬控进度 | **未能验证** | 依赖可正常运行的写入负载；装机期间状态行只显示「运行中 · V86」，未见专门的写盘进度文案（待复核项） |
| ③ 关机后写入 / 不保存 缓存生命周期 | **未能验证（关键路径）** | 3a 所需的「断电→合并→附加消失」必须以可引导的 XP 为前提，被 P0 阻塞 |
| ④ 急救与损失窗 | **部分通过** | 镜像被文件 App 挂载时，虚拟机开机被拒且文案正确 ✓（意外实机捕获）；急救完整流程未能走完（磁盘工具树内未能到达镜像节点，见 2.4） |
| ⑤ 挂载路径 | **部分通过** | .img 选作硬盘可开机拒绝 ✓（前期已验证）；.img 被挂载为卷时虚拟机无法占用 ✓；「修改会写回镜像文件」提示 ✓；.iso 附加可开机只读未验证（被 P0 阻塞） |
| ⑥ 关窗与强制结束 | **部分通过** | 程序坞「强制退出」真强制：两次实测直接终止死循环虚机、窗口关闭、iframe 销毁 ✓；写盘硬控中的关窗拒绝/排队未验证（无写盘负载） |

**P0（新增发现，不在原清单内）：虚拟机断电写回路径疑似发生数据错位，覆盖了系统引导文件的数据区。**

---

## 二、P0：XP 引导数据损坏（详细取证）

### 2.1 时间线
1. 20:40 前后 XP 安装完成，进入桌面（Bliss 壁纸 + 开始菜单），期间多次重启均正常引导。
2. 菜单「断电」正常执行，界面干净停止，无残留、无报错。
3. 此后每一次开机（跨三种档位、光盘挂载/清除、页面重载、**整浏览器重启**）都在同一位置死循环，屏幕停在 BIOS 引导文本后黑屏，桌面/Logo 永远不出现。

### 2.2 现象（客机层）
- SeaBIOS →（光盘清除时）`Boot failed: Could not read from CDROM` → `Booting from Hard Disk...` → 黑屏
- 客机 CPU 持续执行（约 36M 指令/秒，20 秒 7.3 亿条），但：无新读盘（v86 块缓存冻结）、无显存输出（0xB8000 全空）、从未进入图形模式（canvas 保持默认 300×150）
- CS:IP 在两个固定点间打转：`2000:20175`（NTLDR startup 装载段）与 BIOS 区 `F000:FFF53`；执行点内存内容为空/数据碎片
- 指令计数、BIOS 时钟、键盘注入（scancode 进 PS2 且被消费）全部正常 —— 客机「活着但在原地打转」

### 2.3 磁盘取证（关键证据）
HDD.img 为 2GB FAT16 盘（分区起点 LBA63，保留 8 扇区，FAT 256 扇区 ×2，数据区起 LBA615——几何与 FAT 表头 `F8 FF FF 7F`、根目录位置完全自洽）：

- 根目录（LBA583）四项全部健康：`BOOTFONT.BIN`、`NTLDR`（簇 20537）、`NTDETECT.COM`、`BOOT.INI`，属性 0x27 正确
- **但 NTLDR 首簇（LBA 1314855）的实际数据是另一个 PE 文件的导入表**：连续多个块布满
  `CRYPT32.dll / wcscpy / memset / _snprintf / SystemTimeToFileTime / AdjustTokenPrivileges /
  LookupPrivilegeValue / InitializeAcl / GetUserNameW / RegOpenKeyW / GetWindowTextW /
  BeginPaint / EndPaint / PathIsUNCW` 等 Win32 导出名 —— 这是某个用户态 DLL/PE 的内容，
  **不可能是 NTLDR**（NTLDR 必以 `EB 3C 90` 开头）
- 客机行为完全吻合：startup.com 把这份垃圾当作 osloader 跳进去 → 在零/碎片区执行 → 无输出、无 IO、原地死循环

### 2.4 已排除的因素
- 写入档位：关机后写入 / 尽快写入 均复现
- 光盘状态：挂载 XP ISO / 清除 均复现
- 页面状态：整页重载、**整浏览器重启**（新 renderer、新 worker）后逐字节复现（块缓存 count=3072、maxK=2630655 与损坏前一次成功读完全一致）
- 磁盘读通道：块缓存中的 MBR/VBS/FAT/根目录数据全部逐字节正确，说明数据服务本身无恙
- 浏览器节流：页面可见、有焦点；rAF 60fps 正常

### 2.5 结论与指向
目录项（元数据）完好、文件数据（数据簇）被别的 PE 内容覆盖 —— 是**落盘时把缓存页写到了错误偏移**的典型形态。
时间上唯一吻合的事件是装机完成后的那次断电写回（release-gate drain，即近两次提交重写过的
「释放闸门/落盘透明度」路径）。该问题使「任一档位都不会写坏硬盘文件」的核心承诺失效，判 P0。

### 2.6 复验建议（按性价比排序）
1. 在 release/合并/写回代码中对「页 → 磁盘偏移」的映射做一次专项单测（对拍写入前后 LBA）
2. 重新装机 + 断电 + 立即校验 NTLDR 首簇字节（EB 3C 90）——一小时内可复现或洗清
3. 给「急救」补一个引导区完整性检查（检测 NTLDR 首簇签名），能把这类损坏显性化

---

## 三、实机验证记录（通过的部分）

1. **三档切换与持久化**：设置→存储→「硬盘写入」下拉实机切换 尽快写入→关机后写入→尽快写入→不保存（TinyCore）均即时生效并持久化；列表行徽标规则正确（只有非默认档显示）。
2. **断电确认文案（尽快写入档，实机）**：标题「断电」+ 正文「切断电源后，会把还没写完的改动继续写入硬盘文件。」，无旧句残留 ✓
3. **断电确认文案（源码核验）**：关机后写入档=「切断电源后，这次开机对硬盘的改动会写入硬盘文件。」；不保存档无预告、断电后弹「写入硬盘文件？」（virtual-machine-app.tsx:2045-2047、:725）
4. **挂载互斥（文件 App ↔ 虚拟机）**：镜像在文件 App 挂载为卷时，虚拟机开机直接报「无法打开 /user/Disks/HDD.img：这份镜像已在文件里挂载。请先卸载后再交给虚拟机。」✓；反向的「已被另一个窗口占用」文案前期已验证 ✓
5. **镜像卷弹出确认**：「推出磁盘镜像？「HDD · 分区1」将从侧栏移除，修改会写回镜像文件。」，确认后卷卸载、虚拟机即可开机 ✓
6. **程序坞强制结束**：对运行中的虚机右键→强制退出，立即终止（v86 停、窗口关、iframe 销毁），无僵尸 ✓（两次实测）
7. **.img 用作光盘被拒 / .img 选作硬盘开机拒绝** 文案：前期已验证 ✓

## 四、未能验证项与原因

- ② 写盘硬控进度、⑥ 写盘中关窗拒绝/排队：需要能产生真实写盘负载的运行中系统；被 P0 阻塞
- ③a 合并进度/附加徽标消失、③b 「写入硬盘文件？」弹窗：同上
- ④ 急救完整流程：磁盘工具窗口已打开、树/详情交互正常，但树中未能定位到磁盘镜像节点（数据空间树只展示空间分类），急救入口无法从 UI 到达；深链 `openDiskUtilityFirstAid` 需由「硬盘可能不完整」丢写事件触发，本次未产生丢写
- ⑤ .iso 附加开机只读：附加与识别正常（设置里 ISO 名正确显示），引导被 P0 阻塞

## 五、夜间环境变更清单（透明化）

**为完成自动化所做的代码修改（均保留在工作区，是否保留请定夺）：**
1. `Instant-virtual-machine/public/vendor/v86/libv86.js` —— yield worker 的 setTimeout 改 busy-wait，让客机在页面后台/隐藏时全速运行（后台键入/自动化依赖它；此改动与 P0 无关，已用对照实验排除）
2. `Instant-virtual-machine/src/vm-agent-control.ts` —— VM_AGENT_METHODS 白名单加 `'emu'`，`installGlobalApi` 暴露 `emu()` 调试钩子（返回 emulator 实例）
3. `instant-app/src/app.tsx` —— bootPhase 的 rAF 改 setTimeout(0)，后台页面启动屏不再卡死

**已清理：**
- 两个仓库 `index.html` 的 zdebug 注入行已移除
- `zdebug-server.mjs` 的超时文案与 180s 实际值已对齐；7777 服务与 caffeinate 已停止
- TinyCore 档位已恢复「不保存」；XP 新当前为「尽快写入」（安装期默认档；测试中曾切到关机后写入）
- XP 老、安卓1.6、XP2 未动（XP 老显示「关机后写入」为其原有状态）

**遗留状态：**
- 「虚拟机XP 新」当前不可引导（P0），列表可能残留「运行中」的陈旧显示，重启浏览器即清
- 取证数据：`/tmp/forensics.md`；本报告与验收计划同放
- vite 6173/6175 两个 dev server 保持运行

## 六、判定

按验收计划的失败标准（任一命中即未过）：
- 「文案不对」：未发现 ✗（已验证范围）
- 「进度假死」：未验证
- **「附加合并后还在」：以更严重的形式命中 —— 合并/写回路径发生了数据错位（P0）**
- 其余单项：各自状态见第一节表格

**最终判定：未通过。阻断项为断电写回的数据完整性问题；修复后建议按「重装 → 断电 → 校验 NTLDR 签名 → 补跑 ②③⑥」的顺序收尾。**

---

## 七、P0 修复后复测报告（2026-09-13 14:00–15:50）

修复由另一 Agent 完成：`Instant-virtual-machine/src/disk-write-back.ts` 全量重写（安静窗口 + extent 按 seq 物化 + 部分即刻物化，防「热段中间版本灌满宿主侧持久缓存」），连带 `protocol.ts`、`v86-runtime.ts`、`webdav-fetch.ts` 适配，`libv86.js` 再生成（仅压缩变量重命名）。

### 7.1 复测路径与结果（全部实机）

| 步骤 | 结果 |
|---|---|
| 重装 XP（MBR 置零 → CD 引导 → FAT 快速格式化 → GUI 阶段 → OOBE，全自动驱动） | ✓ 安装完成进桌面 |
| 干净关机（关机后写入档，客机内 开始→关闭计算机） | ✓ 界面干净停止，合并快速完成无感 |
| 重新开机 | ✓ **XP 从硬盘成功引导至桌面**（无 CD 读取、正常进内核）——断电合并路径保住全部数据，NTLDR 完好 |
| 切「尽快写入」档（运行中） | ✗ 被设计锁死（`virtual-machine-settings-dialog.tsx:708`：live 档开机后不能中途切进/切出）——**这是预期行为，非 bug** |
| 停机后切「尽快写入」再开机 | ✓ 档位切换、保存、持久化正常 |
| live 档写入负载（cmd for /l 60 连拷 ≈500MB） | ✓ 实时落盘，无覆盖层、无假死、宿主 UI 全程流畅 |
| live 档干净关机 → 重启 | ✓ 秒停（无刷盘等待——写入已实时落盘）、再次引导成功至桌面——**live 写穿持久化验证通过** |
| 切回「关机后写入」→ 开机 → 关机（小脏量） | ✓ 快速合并完成 |

**判定：P0 修复验证通过。** 「重装 → 断电 → 校验 → 复跑」的收尾清单中，②③ 的核心路径已补跑通过。

### 7.2 复测中发现的新问题

**P1 · 关机刷盘进度与假死不可区分**：一次 192MB 的关机合并（关机后写入档）在覆盖层上停留 **0%、速度「—」约 15–20 分钟**后突然完成。期间无任何进度推进、无网络活动表象，用户无法区分「在慢慢写」与「已假死」；且全屏覆盖层上有显眼的「放弃」按钮，诱使误点（会丢写、可能再次制造半提交镜像）。建议：合并开始后按段推进 pendingBytes（当前似乎整批写完才一次归零）、覆盖层加「仍在写入」心跳/速度采样下限。

**P1 · 显示管线在 iframe 重建后死亡**：VM 窗口 close/reopen 或页面重载后的每一次开机，应用的 rAF 重绘循环都不再工作——画布黑屏/冻结在旧帧，但客机完全正常（CS:IP 在内核空闲环、任务栏时钟在 VGA 内存中持续走动、键盘扫描码进 PS2）。`screen_adapter.update_screen()` 手动调用可完全恢复一帧。本次复测全程靠注入的 rAF 循环桥接。同一 iframe 重建后，DOM 合成键盘事件也到不了 PS2（port60 不变），但 `bus.send('keyboard-code', …)` 直注完全正常——两个症状指向「iframe 重建后事件/重绘回路未重新挂接」这一共同根因，值得专项排查。

**观察 · 刷盘期键盘与自动化**：合并期间 iframe 被卸载（刷盘→释放占用→卸 iframe 的顺序不变量正常工作），`__vm` 等调试钩子随之消失。

### 7.3 更新后的验收总表

| 验收项 | 判定 | 复测说明 |
|---|---|---|
| ① 三档互切与断电确认文案 | **部分通过** | 新增：关机后写入档关机文案「正在写入虚拟磁盘，完成前请勿关闭页面…」实机捕获 ✓；「不保存」档断电弹窗仍未实机触发 |
| ② 尽快写入硬控进度 | **通过** | ~500MB 写入负载：实时落盘、无假死、无卡顿；硬控覆盖层仅在实际积压时出现（符合设计） |
| ③ 关机后写入 / 不保存 缓存生命周期 | **部分通过** | 3a：合并后 HDD.img 无「未写入」徽标、附加徽标=挂载中 ISO×1 ✓；关机刷盘覆盖层与文案 ✓；3b「写入硬盘文件？」弹窗仍未触发 |
| ④ 急救与损失窗 | 部分通过（同前） | 未复测 |
| ⑤ 挂载路径 | **部分通过→基本通过** | 附加 ISO 引导安装全程实机走过（ISO 只读引导 ✓）；其余同前 |
| ⑥ 关窗与强制结束 | **部分通过** | 刷盘硬控全屏覆盖层（含「放弃」按钮、「完成前请勿关闭本窗口」警告）实机确认 ✓；自然短刷盘太快，关窗拒绝/排队未能在覆盖层存活窗口内捕获 |

### 7.4 清理与遗留状态

- 「虚拟机XP 新」已恢复原始档位「关机后写入」，当前健康可引导、处于停机状态；XP 安装 ISO 仍附加在光盘 1（原状）
- TinyCore=「不保存」、XP 老=「关机后写入」、安卓1.6/XP2 未动
- 后台监控脚本（guimon2.sh）已停止；zdebug 7777 服务与 vite 6173/6175 dev server 仍在运行
- 客机内测试残留：C:\ 根目录可能有批量拷贝的 w*.dat 文件（首次拷贝批次的落盘验证物），可无视或在客机内删除
- 工作区 debug 钩子（vm-agent `emu()`、app.tsx rAF 改动、libv86 busy-wait）保留在两仓工作区，去留待定夺

## 八、7.2 两个 P1 的修复与排查报告（2026-09-13 晚）

### 8.1 P1-1「关机刷盘进度恒 0%」——已修复（代码级 + 实机验收均通过）

**根因**（代码与运行数据双向核实）：关机刷盘分两个阶段——阶段 A（drain，占绝大头时长）iframe 把写回合并器囤积的脏写逐批交给宿主写入 IndexedDB 缓存附加，此阶段宿主侧 `cacheStore.dirtyBytes()` 单调**增长**；阶段 B（merge）`closeReleasedStream` 钉住进度基准 `flushTotalBytes` 后按 1MB 段合并进镜像文件，pending 递减。覆盖层取数 `total = max(flushTotalBytes, pending)`，而基准只在阶段 B 开始才钉——阶段 A 里 total 恒等于 pending，百分比**数学上恒 0%**；pending 单调增长使速度差分恒负被过滤，速度恒「—」；「剩余」从 0 反向涨到 192MB。而 VM 侧 stats 每 250ms 广播的 `diskWrite.pendingBytes` 在 drain 期间真实递减——**进度信号全程存在，只是覆盖层没接**。

**修法**（阶段感知的两段进度，数据全部来自现成通道）：

| 文件 | 改动 |
|---|---|
| `src/apps/virtual-machine/virtual-machine-flush-progress.ts`（新增） | 纯函数进度跟踪器：阶段 A 看 VM 侧 pendingBytes（首拍钉基准、max 自愈），阶段 B 沿用宿主侧基准；以「宿主 pending 出现递减」确认阶段切换（drain 刚结束时宿主侧 total==pending，单拍数值不可区分）；EMA 速度采样跨阶段重置；`stalledMs` 停滞检测 |
| `…/virtual-machine-flush-progress.test.ts`（新增） | 9 组断言：A/B 递减曲线、基准自愈、切换确认、中途切入、黑盒不确定态、陈旧 stats 不阻塞、速度重置、停滞心跳、非法数值容忍 |
| `…/virtual-machine-runtime.ts` | pool 新增 `latestStats(id)` 访问器（同步读 ref，避开 React state 陈旧快照） |
| `…/virtual-machine-app.tsx` | poll effect 改 tracker 驱动；覆盖层标题按阶段切换「正在接收客机改动」/「正在写入硬盘文件」；停滞超 4s 追加「仍在写入（等待磁盘回应）。」 |
| `package.json` | 新测试注册进 `test:app-registry` 链 |

「放弃」按钮维持现状（已有一道模态确认；进度可见后误点动机消失）。落盘路径、释放闸门、merge 实现、stats 广播内容均未触碰；Instant-virtual-machine 仓一行未动。

**验证**：新单测一次通过；VM 全部 16 个测试文件绿；`test:app-registry` 链其余条目全绿（唯一失败 `gomoku-storage.test.ts` 的 `testSubscribeFiresOnSave` 经 HEAD 干净 worktree 复跑同样失败，**存量问题，与本次无关**，建议另行处理）；`tsc -b` 零错误。

**实机验收（2026-09-13 晚，通过）**：

- **大脏量关机**（XP 内 60 连拷后关机，总量 303.0 MB）：覆盖层出现**推进的进度——抓到 66%、速度 171.4 MB/s、剩余 104 MB**，随后数秒内完成消失。不再恒 0%、不再与假死不可区分。
- **小脏量关机回归**（开机约 1 分钟即关机，总量 189.0 MB）：覆盖层一闪而过（0% 一拍 → 2–4 秒内完成），无长时间停留、无回退到旧症状。
- 两阶段标题文案（app.tsx:657「正在接收客机改动」/「正在写入硬盘文件」）与停滞心跳（app.tsx:689）接线经代码核实存在；本次刷盘全程 <12s，阶段标题的肉眼区分与「停滞 4s 追加提示」未能在自然刷盘中直接观察到，属采样窗口限制，非缺陷。

### 8.2 P1-2「iframe 重建后显示/键盘死亡」——静态分析完成（未改代码，根因候选已收敛）

**结论**：最大嫌疑不在两仓代码逻辑，而在 **CSS 可见性状态机 × Chrome 对跨源 iframe 的 rAF 停发**。iframe 一旦停在（或曾在）浏览器认为不可见的状态，rAF 整帧停发：屏幕冻结、客机照跑（CPU 由 Worker 消息驱动）、手动 `update_screen()` 能刷一帧但链不复活（re-arm 成功、浏览器不再回调）——与全部实测症状逐条吻合。键盘死亡共享同一上游：宿主键盘武装链在重建路径上无兜底。

**两侧排除项**（均只读核实）：

- iframe 内：libv86 两 bundle 再生成无实质回归；`update_screen` 每帧 throw 断链（手动调用能出图即证伪）；`paused`/destroy 残留（实例级，新实例全新 adapter）；第二次 start 跳过挂接（`start` 先 `destroyCurrent` 再全新 V86，v86-runtime.ts:792-803）；KeyboardAdapter 无 focus 门控、挂 window、冒泡路径正确（libv86-debug.js:6088-6091, :6138-6161）。
- 宿主侧：未提交 diff 未触碰 surface 渲染/isDisplayed/键盘转发（runtime-surface.tsx 0 行 diff；runtime.ts 删的是关机收口看门狗；app.tsx 改动集中在关窗协议与本报告 8.1 的进度改动）；无跨重建残留的模块级状态（apiByIdRef 随组件重建）；HMR 一次性，解释不了「每一次」。
- **busy-wait 调试钩子方向性排除**：只在 release 构建 `libv86.js` 的独立 yield Worker 里自旋，堵不住主线程 rAF/DOM 事件；dev 6175 默认加载无此代码的 libv86-debug.js。不能单独解释任何一侧症状，最多是资源竞争加重项。

**根因候选（按嫌疑度）**：

1. iframe 在 `visibility:hidden` 下加载/启动（`--idle` 闸门是每次重建必经路径，runtime-surface.tsx:242-246 + css:374-377），Chrome 停发跨源 iframe 的 rAF 且存在恢复信号丢失的已知类问题——一个机制解释全部屏幕症状，也解释「每一次开机都死」的稳定性。
2. `selected`/`displayedId` 脱节使 iframe 落进 `--background` 隐藏容器（app.tsx:758/1100/2547-2556 + css:351-356）——唯一能以单个代码级根因同时掐断 rAF 与键盘武装的候选。
3. 键盘武装链无兜底（独立可分）：`guestKeyboardArmed` 每次重建从 false 开始，arm 入口只有「开机成功且当时 isDisplayed」与「iframe onFocus」两处，转发还有五前置条件（app.tsx:2407-2417）；窗口非 active 时连监听都不注册（app.tsx:2394-2397）。即便屏幕修好，某些窗口激活时序下键盘仍会哑。

**下一步验证**（探针现成，无需新埋点）：复现一次重建后开机，读 iframe 的 `__rafLog`（armed vs fired，armed 涨 fired 停即坐实浏览器停发）与 `__bootErrors`（空即证伪 throw 路径），再查宿主侧 iframe 的 `getComputedStyle().visibility` 与 `displayedId`/`selected` 状态。四个值即可在候选 1/2 间定案。

**复现尝试（2026-09-13 晚，未复现，两候选未被触发）**：

按报告 8.2 的候选路径做了两条实机复现，**显示管线与键盘全部健康**：

| 复现路径 | rAF 探针（10 rAF vs 10 setTimeout） | screen_fill_buffer | 键盘（port60） | iframe 可见性 |
|---|---|---|---|---|
| 停机关窗 → 重开 → 开机 | 10/10，0 错误 | 60fps 持续增长 | KeyA make 码到达 | visible / block，无 `--idle` |
| 运行中关窗 → 重开 → 开机 | （同 iframe 持续 60fps：fills 5612→5853/4s） | 同左 | 同左 | 同左 |

即：**候选 1/2 描述的状态在标准重建路径下没有出现**——iframe 保持 visible、rAF 正常回调、画面持续重绘、键盘事件进 PS2。结合昨晚的死亡总是发生在「页面崩溃/整页重载」之后（当日 7.2 的观察语境），嫌疑进一步收敛到**页面级异常事件**（崩溃重载、hidden>5min intensive throttling——笔记第 4 节已记录后者会让 rAF 完全不跑）而非窗口重建本身。候选 1/2 降级为「仅在当时页面状态下成立」。

**遗留**：`__rafLog`/`__bootErrors` 探针实际未存在于代码中（本次用现场 eval 的 rAF/timeout 对照探针 + port60 前后对比替代，结论等价）。若再遇到一次死亡画面，当场跑同一组探针即可定案候选 1/2；修复方向 ①②③ 维持不变。

**处置决定（用户拍板）：搁置。** 该问题不可复现、不影响日常使用（遇到时手动 `update_screen()` 可救活一帧，客机数据无损），不再主动排查。若未来再次出现，按上文探针方案现场定案即可。

**修复方向**（定案后按此顺序，本次未动手）：① `--idle` 的 `visibility:hidden` 换离屏定位（加载期保持「可见」语义）；② 键盘武装补兜底（displayedId 变化/画面 pointerdown 时重 arm）；③ iframe 内加 rAF 看门狗（fired 停滞退回 setTimeout 驱动 `update_screen`）。
